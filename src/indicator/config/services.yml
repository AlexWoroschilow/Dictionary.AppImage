services:
  indicator.subscriber_gui:
    class: src.indicator.kernel.AppListener
    calls:
         - [ set_container, [ "@service_container" ]]
    tags:
        event.listener:
         - { name: 'indicator.menu', method: 'OnMenuIndicator', priority: 10 }
         - { name: 'indicator.menu', method: 'OnMenuClipboard', priority: 20 }
         